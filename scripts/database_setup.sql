-- Execute this file in your MySQL console or a MySQL GUI client (like phpMyAdmin/DBeaver)
-- Assumes standard local MySQL setup (User: root, Password: "")

CREATE DATABASE IF NOT EXISTS college_connect CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Note: The settings.py connects to MySQL with user 'root' and an empty password.
-- If your local machine uses a different setup, modify `college_connect/settings.py` DATABASES config before proceeding.
